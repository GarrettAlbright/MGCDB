<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="$linkBase/static/css/styles.css">
    <title>Mac Game Compatibility Database#if($title) - $title#end</title>
  </head>
  <body>
    <aside class="auth-box">
      #if($authenticated)
        Logged in as <img src="$userSteamAvatar" alt="Steam Avatar" class="steam-avatar" width="15" height="15"> $userSteamNickname - <a href="$linkBase/user/log-out" title="Log out of MGCDB">Log out</a>
      #else
        <a href="$baseUrl/auth" title="Log in to MGCDB with Steam"><img src="$linkBase/static/img/sits_small.png" width="154" height="23" alt="Sign In Through Steam icon"></a>
      #end
    </aside>
  	<h1>MGCDB</h1>
    #parse($template)
  </body>
</html>
